# formatR6

requires duckdb, install with
```
pip install duckdb
```

run with:
```
python path/to/formatR6.py path/to/datafile.txt
```

formatted file will be saved as `path/to/out_datafile.csv`


help with:
```
python path/to/formatR6.py -h
```


